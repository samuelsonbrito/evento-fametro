# Credenciais reais — nunca versionar. config/database.php agora só lê de .env
# (via includes/env.php), então ele mesmo pode ser versionado normalmente.
.env
.env.*
!.env.example

# Docker (harness)
harness/config.local.php.bak

# SO / editores
.DS_Store
Thumbs.db
.vscode/
